{
  "version": "1.0",
  "name": "Beacon Dashboard",
  "description": "Multi-dashboard civic monitoring system",
  "rules": [
    "Always verify TypeScript compiles without errors",
    "Run lint before committing",
    "Follow existing component patterns"
  ],
  "env": {
    "required": [],
    "optional": ["NEXT_PUBLIC_API_URL"]
  }
}
